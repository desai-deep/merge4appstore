# App Store Connect API credentials (required)
APP_STORE_CONNECT_API_KEY_ID=
APP_STORE_CONNECT_ISSUER_ID=
APP_STORE_CONNECT_API_KEY_CONTENT=

# GitHub token for PR comments (required for gh CLI)
GH_TOKEN=

# iOS repo path (optional - only needed for release sync to create git tags)
# IOS_REPO_PATH=/path/to/your/ios-repo

# App configuration (optional - defaults shown)
# APP_ID=                            # App Store Connect app ID (use if bundle ID matches multiple apps)
# APP_BUNDLE_ID=com.example.myapp
# APP_NAME=My App
# GITHUB_REPO_OWNER=my-org
# GITHUB_REPO_NAME=my-ios-repo
# XCODE_WORKFLOW_ID=

# Dry run mode (optional)
# DRY_RUN=true
